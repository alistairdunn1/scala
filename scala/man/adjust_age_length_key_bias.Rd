% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/length_bias_correction.R
\name{adjust_age_length_key_bias}
\alias{adjust_age_length_key_bias}
\title{Create Bias-Adjusted Age-Length Key}
\usage{
adjust_age_length_key_bias(
  age_length_key,
  bias_factors = NULL,
  modulus = 5,
  bias_strength = 1.5
)
}
\arguments{
\item{age_length_key}{Data frame with columns: length, age, proportion}

\item{bias_factors}{Data frame with columns: length, bias_factor
(bias_factor > 1 = over-measured, < 1 = under-measured)}

\item{modulus}{Numeric, modulus for automatic bias factor calculation (default 5)}

\item{bias_strength}{Numeric, strength of automatic bias (default 1.5)}
}
\value{
Adjusted age-length key with corrected proportions
}
\description{
Adjusts age-length key proportions to account for known length measurement bias.
}
\examples{
\dontrun{
# Create age-length key
alk <- generate_age_length_key(length_range = c(20, 40), age_range = c(1, 8))

# Create bias pattern (multiples of 5 over-represented)
bias_pattern <- data.frame(
  length = 20:40,
  bias_factor = ifelse((20:40) \%\% 5 == 0, 1.5, 1.0)
)

# Adjust age-length key
adjusted_alk <- adjust_age_length_key_bias(alk, bias_pattern)
}

}
