% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.scaled_length_composition.r,
%   R/print.scaled_length_frequency.r
\name{print.scaled_length_composition}
\alias{print.scaled_length_composition}
\title{Print Scaled Length Composition Results (Sex-Based)}
\usage{
\method{print}{scaled_length_composition}(x, show_cvs = TRUE, show_sexes = TRUE, digits = 2, ...)

\method{print}{scaled_length_composition}(x, show_cvs = TRUE, show_sexes = TRUE, digits = 2, ...)
}
\arguments{
\item{x}{An object of class \code{scaled_length_composition} as returned by \code{calculate_scaled_length_compositions}}

\item{show_cvs}{Logical, whether to display coefficient of variation (CV) columns (default TRUE)}

\item{show_sexes}{Logical, whether to display all sex categories (default TRUE)}

\item{digits}{Integer, number of digits to round counts and proportions (default 2)}

\item{...}{Additional arguments (not used)}
}
\value{
Invisibly returns a list of printed data frames for pooled and stratum-specific results. Each data frame contains length composition counts by sex, and optionally CV columns if available.

Invisibly returns the printed data frames.
}
\description{
Prints a summary of scaled length composition results by sex, including pooled and stratum-specific compositions, proportions, and uncertainty estimates (CVs) if available.

Prints a summary of scaled length composition results by sex, including pooled and stratum-specific compositions, proportions, and uncertainty estimates (CVs) if available.
}
\details{
The function prints:
- Pooled length composition results by sex (male, female, unsexed, total)
- Stratum-specific length composition results if multiple strata are present
- Summary statistics for total, male, female, and unsexed fish
- CV columns if \code{show_cvs = TRUE} and uncertainty estimates are available
}
\examples{
\dontrun{
test_data <- generate_test_data()
results <- calculate_scaled_length_compositions(
  fish_data = test_data$fish_data,
  strata_data = test_data$strata_data,
  length_range = c(15, 35),
  bootstraps = 100
)
print(results)
}

\dontrun{
test_data <- generate_test_data()
results <- calculate_scaled_length_frequencies(
  fish_data = test_data$fish_data,
  strata_data = test_data$strata_data,
  length_range = c(15, 35),
  bootstraps = 100
)
print(results)
}

}
