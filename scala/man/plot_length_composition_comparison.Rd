% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_length_composition_comparison.R
\name{plot_length_composition_comparison}
\alias{plot_length_composition_comparison}
\title{Plot multiple length composition objects in a multi-panel layout}
\usage{
plot_length_composition_comparison(
  x,
  names = NULL,
  by_stratum = FALSE,
  stratum = NULL,
  show_CIs = TRUE,
  type = "composition",
  length_bin_size = NULL,
  unsexed = FALSE,
  ...
)
}
\arguments{
\item{x}{List of objects of class \code{length_composition}}

\item{names}{Character vector of names for each list element (optional). If NULL, uses list names or creates default names.}

\item{by_stratum}{Logical, whether to plot by stratum (TRUE) or pooled across strata (FALSE). Default is FALSE.}

\item{stratum}{Character, specific stratum name to plot when by_stratum = TRUE. If NULL, uses first stratum. Ignored when by_stratum = FALSE.}

\item{show_CIs}{Logical, whether to show 95 percent confidence interval ribbons if bootstrap results are available. Default is TRUE.}

\item{type}{Character, either "composition" or "proportion" (or partial matches). Default is "composition".}

\item{length_bin_size}{Numeric, size of length bins in cm for aggregating data (e.g., 2, 5). If NULL (default), no binning is performed.}

\item{unsexed}{Logical, whether to include unsexed fish category in the plot. Default is FALSE.}

\item{...}{Additional arguments (not used)}
}
\value{
A ggplot object with faceted layout (rows = list elements, columns = sex categories)
}
\description{
Creates a multi-panel ggplot visualization where each row represents a different
length composition result and columns show sex categories.
}
\examples{
\dontrun{
# Create multiple results
result1 <- calculate_length_compositions(...)
result2 <- calculate_length_compositions(...)
results_list <- list("Scenario A" = result1, "Scenario B" = result2)

# Plot comparison
plot_length_composition_comparison(results_list)

# Plot specific stratum comparison
plot_length_composition_comparison(results_list, by_stratum = TRUE, stratum = "North")
}

}
